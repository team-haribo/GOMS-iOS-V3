# 에러가 아무리 많아도 빌드는 성공시키기
warning_threshold: 1000

disabled_rules:
  - identifier_name
  - line_length
  - type_name
  - trailing_whitespace
  - function_body_length
  - force_cast          # 강제 캐스팅 에러 방지
  - unneeded_override   # 아까 터미널에 뜬 경고 방지
  - statement_position

included:
  - Projects
